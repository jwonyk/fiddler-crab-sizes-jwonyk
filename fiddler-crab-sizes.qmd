---
title: "Fiddler crabs and Bergmann’s rule"
author: "J"
format: html
execute: 
  echo: false
---
```{r setup, include=FALSE}
library(lterdatasampler)
library(tidyverse)
library(janitor)
library(here)
library(tidyr)
library(dplyr)
library(lubridate)
library(usethis)
library(metajam)
```

```{r}
#| fig-cap: "this is my caption"

crab_data <- pie_crab %>%
  group_by(site) %>% 
  summarise(mean = round(mean(size, na.rm = TRUE), 2), 
            std = round(sd(size, na.rm = TRUE), 2),
            ss = n())

ggplot(data = pie_crab, aes(x = latitude, y = size)) +
  geom_point()

crab_data
```

